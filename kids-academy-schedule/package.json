{
  "name": "kids-academy-schedule-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && cd www && npm install && cd backend && npm install",
    "dev": "concurrently \"cd frontend_source && vite\" \"cd www/backend && node server.js\"",
    "build": "cd frontend_source && npm run build",
    "start": "cd www/backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
